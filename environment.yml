name: droplet_microfluidics
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - python
  - jupyterlab
  - pip:
    - imagecodecs
    - ipympl
    - matplotlib
    - numpy
    - opencv-python-headless
    - pandas
    - scikit-image
    - tifffile
    - tqdm
